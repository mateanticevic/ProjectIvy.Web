export interface MovieFilters {
    from?: string,
    to?: string,
    pageAll?: boolean,
    ratingHigher?: number,
    ratingLower?: number,
    runtimeLonger?: number,
    runtimeShorter?: number,
}
